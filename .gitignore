node_modules/
backend/node_modules/
frontend/node_modules/
.venv/
__pycache__/
*.pyc
.pytest_cache/
.ruff_cache/
dist/
build/
nohup.out
.DS_Store
.env
.env.local
.env.*
*.env
# ...except the templates, which exist to be committed. Without this exception
# the file documenting the required variables is ignored by the rule protecting
# the file holding their values.
!.env.example
!**/.env.example
emails.txt
*.spec~
.vercel
.vercel_disabled*

# Secrets / credentials — NEVER commit
keys/
client_secret*.json
*.apps.googleusercontent.com.json
*.pem
*.key

# Supabase CLI local state
supabase/.temp/

# Generated frontend build output (Vercel + Docker rebuild it from source)
src/tvlc/static/

# Local DAW / design project files (kept local)
veedeeoh Project/
*.als
*.asd
*.zip
Untitled.*

# Branding source assets (kept local)
Video bump ident design/
Veedeeoh Bump.mp4
Video bump ident design 2/
Veedeeoh Kids Bump.mp4
veedeeohbump.mp3
veedeeohkids.mp3

# Debris from Playwright MCP browser sessions, not part of the app.
.playwright-mcp/
